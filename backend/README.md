# Backend (Express + MongoDB)

1. Install dependencies

```bash
cd backend
npm install
```

2. Start server (ensure MongoDB is running)

```bash
MONGO_URI=mongodb://localhost:27017/mean_user_dashboard npm run dev
```
